{
  "name": "server",
  "version": "1.0.0",
  "description": "1. Copy `env.example` file as a new `.env` file in the root directory. Replace the paths in the `.env` file as instructed.\r 2. Run `npm install`.\r 3. Run `npm start` to start the API server.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.12.0",
    "express": "^4.18.2",
    "graphql": "^16.6.0",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
